name: Diagnostic Probe

on:
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4
      - name: Confirm real cursor pagination mechanics
        run: python3 _diag_probe.py
